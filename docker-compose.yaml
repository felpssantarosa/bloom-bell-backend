services:
  backend:
    container_name: bloom-bell-backend
    image: felipessdev/bloom-bell-backend:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3333:3333" # HTTP
      - "3334:3334" # WebSocket
    env_file:
      - .env
    restart: unless-stopped
