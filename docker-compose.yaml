services:
  backend:
    container_name: bloom-bell-backend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3333:3333" # HTTP
      - "3334:3334" # WebSocket
    env_file:
      - .env
    restart: unless-stopped
